export type PartenairesType = {
    path: string;
    alt: string;
}

export type NavLinkType = {
    url: string;
    label: string;
}
export type SocialItemType = {
    label: string,
    url: string,
    icon: string
}