---
const currentLang = Astro.url.pathname.startsWith("/fr") ? "fr" : "en";
const switchLang =
  currentLang === "en"
    ? "/fr" + Astro.url.pathname.slice(3)
    : "/en" + Astro.url.pathname.slice(3);
const label = currentLang === "en" ? "🇬🇧 English" : "🇫🇷 Français";
const altLabel = currentLang === "en" ? "🇫🇷 Français" : "🇬🇧 English";
---

<div class="absolute left-2 top-2">
  <a href={switchLang} class="rounded border px-2 py-1 transition">
    {altLabel}
  </a>
</div>
